USE sale_management;

CREATE TABLE Customers (
    customer_id INT, first_name VARCHAR(50), date_of_birth DATE
);

CREATE TABLE SALES (
    customer_id INT, first_name VARCHAR(50), date_of_birth DATE
);

SHOW TABLES;
